 namespace Example {
   export class Amount {
        // tslint:disable-next-line:variable-name
        private readonly _id: string;

        constructor(id: string) {
            this._id = id;
        }
    }
}
